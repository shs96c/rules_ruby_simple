RubyInfo = provider(
    fields = {
        "transitive_sources": "`depset` of `Files` containing all transitive sources",
        "paths": "`depset` of `string` containing directories to add to the `LOAD_PATH",
    },
)
